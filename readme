abrir .env criptografado

openssl enc -d -aes-256-cbc -in .env.enc -out .env -pass pass:"SENHA USADO PARA CRIPTOGRAFAR"

abrir Anotacoes.txt criptografado

openssl enc -pbkdf2 -salt -in Anotacoes.enc -out Anotacoes.txt -pass pass:"SENHA USADO PARA CRIPTOGRAFAR"